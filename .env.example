# Misbaha — Zikr Apps (salam@zikrapps.com)
# No required environment variables for local development.
# All user data stays on-device (AsyncStorage).
#
# Optional CI only (set in GitHub Actions secrets, never commit):
# EXPO_TOKEN=your-expo-access-token-for-eas-build
