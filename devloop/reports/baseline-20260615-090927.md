# DevLoop benchmark — baseline-20260615-090927

- Phase: **baseline**
- Generated: 2026-06-15T16:32:40Z
- Commit: `fcbd0042ffff`
- Device: `unset`

## Totals

| Metric | Count |
|--------|------:|
| Planned | 150 |
| Completed | 150 |
| Passed | 150 |
| Failed | 0 |
| Pass rate | 100.0% |

## By scenario

| Scenario | passed | failed | skipped | blocked |
|----------|-------:|-------:|--------:|--------:|
| scenario-count | 30 | 0 | 0 | 0 |
| scenario-goal | 30 | 0 | 0 | 0 |
| scenario-home | 30 | 0 | 0 | 0 |
| scenario-lifecycle | 30 | 0 | 0 | 0 |
| scenario-search | 30 | 0 | 0 | 0 |

## Run history (smoke → full baseline)

- **Stage 1 — Initial smoke (fast reset):** All runs errored (`execution_error`). Reason: harness targeted the wrong bundle id. Fix: set `APP_BUNDLE=com.zikrapps.misbaha`. Result: routines could launch the app.
- **Stage 2 — 5-sim parallel smoke:** 1/5 passed (`postcondition_timeout`). Reason: CPU/RAM contention from 5 simulators on a 16 GB M4. Fix: cut to 2 parallel sims. Result: fewer timeouts, still flaky.
- **Stage 3 — Bundle mismatch in build:** Installed app was `com.misbaha.app`. Fix: set `PRODUCT_BUNDLE_IDENTIFIER=com.zikrapps.misbaha`, rebuild + reinstall. Result: bundle id consistent across app and routines.
- **Stage 4 — Deep-link dialog crash:** Smoke 1/5; "Something went wrong" on deep-link nav. Reason: iOS "Open in 'Misbaha'?" confirm dialog blocked test deep links. Fix: `hard_reset` (terminate+launch) + `confirm_scheme_dialog`; add ErrorBoundary logging. Result: dialogs auto-confirmed, errors visible.
- **Stage 5 — Search via dialog:** `search` still failed at the same dialog. Fix: `search_dua` taps `tab-tasbeeh` instead of `open_url`. Result: search avoids the confirm dialog.
- **Stage 6 — Router race on cold launch:** `goal` failed "Attempted to navigate before mounting the Root Layout". Reason: expo-router navigate-before-mount race in E2E routes. Fix: defer `router.replace` via `requestAnimationFrame` in `test/reset.tsx` and `test/go/[tab].tsx`. Result: cold-launch nav stable.
- **Stage 7 — Stuck device leases:** Re-smoke blocked on `device-busy`. Reason: stale leases from force-killed runs. Fix: clear rogue processes, wait for lease expiry, manual release. Result: devices freed.
- **Stage 8 — Metro bundle overlay:** "Downloading 100%…" overlay caused flakiness. Reason: dev build depended on the Metro JS bundle. Fix: switch to a Release build with an embedded bundle. Result: no Metro dependency at runtime.
- **Stage 9 — Stale count routine:** `count` failed `element_not_found` ("Expand dua"). Reason: routine referenced a removed category and timing-fragile double taps. Fix: rebuild `expand_dua_and_count` v8 (open Quranic Duas + 3 s hold-to-complete). Result: deterministic counting.
- **Stage 10 — First full baseline:** 94.7% (142/150); `goal` 7/30 `element_not_found`. Reason: `surprise-goal-button` tapped before the Goals screen rendered under load. Fix: harden `start_surprise_goal` v4 (`wait_for: screen_changed`). Result: goal nav resilient to slow renders.
- **Stage 11 — Splash readiness race:** Rerun regressed to 84.6%; 22/23 failures showed only the "Misbaha" splash. Reason: fixed reset sleep too short under load. Fix: add `wait_until_ready` poll to `hard_reset`. Result: routines start on a rendered app.
- **Stage 12 — Bridge connection drops:** Persistent `execution_error` ("All connection attempts failed"). Reason: DevLoop bridge/WDA instability under sustained parallel load. Fix: `apply_with_retry` with backoff + `recover_to_today`. Result: transient flakes absorbed; hard WDA crashes persisted.
- **Stage 13 — WDA crash root cause:** Crash report showed WDA aborting in `-[FBWebServer startHTTPServer]`. Root cause: the bridge spawned duplicate WebDriverAgent launchers per device → WDA HTTP-port collision; aggravated by 2 sims + orphan leases. Fix: move to single-sim sequential to isolate. Result: root cause confirmed.
- **Stage 14 — Clean run:** Restarted QualGent Desktop (cleared duplicate WDA + stale leases), ran single-sim sequential. Result: **150/150 (100%)**, zero connection errors.

## Issues, fixes, and categories

| Issue | Fix | Category |
|-------|-----|----------|
| Harness targeted wrong bundle id | Set `APP_BUNDLE=com.zikrapps.misbaha` | Test harness / config |
| 5-sim parallel timeouts | Reduce to 2, then 1 (sequential) | Parallel vs single-thread execution / hardware |
| App built with `com.misbaha.app` | Fix `PRODUCT_BUNDLE_IDENTIFIER`, rebuild | Build / tooling |
| iOS "Open in Misbaha?" dialog blocked deep links | `hard_reset` + `confirm_scheme_dialog`; tab-testID nav | Test harness / reset logic |
| expo-router navigate-before-mount crash | Defer `router.replace` via `requestAnimationFrame` | Real app bug |
| ErrorBoundary swallowed errors silently | Add `componentDidCatch` logging | Real app bug |
| Stale device leases (`device-busy`) | Clear processes, wait expiry / bridge restart | DevLoop infrastructure |
| "Downloading 100%…" Metro overlay | Release build with embedded JS bundle | Build / tooling |
| Stale `count` routine selectors/gesture | Rebuild v8 (Quranic Duas + 3 s hold) | Test routine maintenance |
| `goal` tapped before screen rendered | Harden v4 with `wait_for: screen_changed` | Test routine / timing |
| Cold-launch splash race | `wait_until_ready` poll in `hard_reset` | Test harness / reset logic |
| Transient bridge disconnects | `apply_with_retry` backoff + recover | DevLoop infrastructure |
| Duplicate WDA launchers → port collision/crash | Single-sim sequential + restart QualGent Desktop bridge | DevLoop infrastructure |
